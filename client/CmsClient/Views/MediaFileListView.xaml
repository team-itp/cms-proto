<UserControl x:Class="CmsClient.Views.MediaFileListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CmsClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer>
        <local:FlowControl 
            x:Name="flow"
            ItemsSource="{Binding Files}" 
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch">
            <local:FlowControl.ItemTemplate>
                <DataTemplate>
                    <local:MediaFileView Height="280" Width="{Binding ElementName=flow, Path=ActualWidth}"/>
                </DataTemplate>
            </local:FlowControl.ItemTemplate>
        </local:FlowControl>
    </ScrollViewer>
</UserControl>
